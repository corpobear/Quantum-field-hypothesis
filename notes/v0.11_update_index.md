# v0.11 Update Index

**Status:** repository index note  
**Date:** 2026-06-17

v0.11 adds a bounded availability factor for the fourth-mode reservoir and tests the integrated field-source equation.

New files:

```text
models/field_source_reservoir_v0.11.md
tests/report_v0.11.md
```

Core availability rule:

$$
R_4^{gate}=\frac{R_4}{R_4+R_*}
$$

This keeps the reservoir contribution bounded:

$$
0\le R_4^{gate}\le1.
$$

Reduced field-source test:

$$
\Sigma_\tau
=
W_v
\left[
\frac{1}{56}+\frac{7}{8}\frac{1}{448}\langle O_\varphi\rangle
\right]
R_4^{gate}
$$

$$
m_\tau=m_\tau^{base}(1+\Sigma_\tau)^2
$$

At saturated availability:

$$
R_4^{gate}=1,
$$

with optimal funnel capture:

$$
W_v=1,
$$

and:

$$
\langle O_\varphi\rangle=0.9837806705,
$$

the reduced test gives:

$$
m_\tau=1776.86\ \mathrm{MeV}.
$$

Interpretation:

```text
reservoir -> bounded availability
funnel aperture -> tiny captured fraction
Higgs overlap -> conversion condition
speed window -> capture/scatter condition
knot -> amplitude accumulator
mass -> squared amplitude
```

The README should be updated later to list v0.11 in the main repository layout.
