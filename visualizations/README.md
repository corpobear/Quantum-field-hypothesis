# MCIFT Visualizations

**Status:** offline visual development folder for v1.00 mechanics.

## 01 - First Principle Stability Modes

Generated by:

```bash
python visualizations/01_generate_first_principle_modes.py
```

Outputs:

```text
01a_first_principle_mode_1_point.gif
01b_first_principle_mode_2_stable_pair.gif
01c_first_principle_mode_3_stable_threefold.gif
01d_first_principle_mode_4_failed_dissolve.gif
01_first_principle_modes_contact_sheet.png
```

Meaning:

```text
Mode 1: stable point seed
Mode 2: stable two-body loop
Mode 3: stable threefold loop / surface activation
Mode 4: failed fourth mode dissolving outward, leaving central residue/load
```

These are schematic mechanics visualizations, not experimental evidence.

## 02 - Light Projection Reservoir Shell

Generated by:

```bash
python visualizations/02_generate_light_projection_reservoir_shell.py
```

Outputs:

```text
02_light_projection_reservoir_shell.gif
02_light_projection_reservoir_shell_preview.png
```

Meaning:

```text
The stable third mode remains coherent inside the boundary.
The failed fourth mode dissolves outward into the background reservoir.
The outer shell is populated from outside by the reservoir, not emitted from the inner threefold mode.
```

## 03 - Threefold Wobble Creates Sphere

Generated by:

```bash
python visualizations/03_generate_threefold_wobble_sphere.py
```

Outputs:

```text
03_threefold_wobble_creates_sphere.gif
03_threefold_wobble_creates_sphere_preview.png
```

Meaning:

```text
The stable third mode begins as a flat threefold spin.
The spin axis wobbles through x, y, and z directions.
The accumulated sweep fills a sphere, not only a flat bubble outline.
```

## 04 - Outer Shell Feeds Core

Generated by:

```bash
python visualizations/04_generate_core_from_outer_shell.py
```

Outputs:

```text
04_outer_shell_feeds_core.gif
04_outer_shell_feeds_core_preview.png
```

Meaning:

```text
The sphere and outer reservoir shell remain established.
Mass flows inward from the shell toward the center.
The center condenses into a core without consuming the stable threefold loop.
```

## 05 - Core Vibration Lights the Shell

Generated by:

```bash
python visualizations/05_generate_core_vibration_light_to_shell.py
```

Outputs:

```text
05_core_vibration_light_to_shell.gif
05_core_vibration_light_to_shell_preview.png
```

Meaning:

```text
The core continues receiving mass from the outer shell.
The core begins vibrating/pulsing.
The vibration emits light outward until the shell responds.
This creates the first visible core-shell feedback loop.
```

## 06 - Shell Light Cones and Ripples

Generated by:

```bash
python visualizations/06_generate_shell_light_cones_ripples.py
```

Outputs:

```text
06_shell_light_cones_ripples.gif
06_shell_light_cones_ripples_preview.png
```

Meaning:

```text
Core light reaches the shell in three cones.
The shell responds locally with water-like ripples.
The ripple response begins organizing into the A3 surface activation pattern.
```

## 07 - Spectral Shell Growth

Generated by:

```bash
python visualizations/07_generate_spectral_shell_growth.py
```

Outputs:

```text
07_spectral_shell_growth.gif
07_spectral_shell_growth_preview.png
```

Meaning:

```text
The shell begins growing outward.
The core vibration changes frequency and emits different colors of light.
The growing shell preserves that frequency history as colored bands.
The threefold cone pattern is retained in the growth texture.
```

## 08 - Core Gravity Stabilizes Shell Pattern

Generated by:

```bash
python visualizations/08_generate_gravity_stabilizes_shell.py
```

Outputs:

```text
08_gravity_stabilizes_shell_pattern.gif
08_gravity_stabilizes_shell_pattern_preview.png
```

Meaning:

```text
The core receives more shell-fed mass and densifies.
The dense core creates a gravity-like stabilizing field.
Gravity locks the outer shell's spectral/threefold pattern.
Update frequency slows with radius, so outer shell layers change more slowly than inner layers.
```

## 09 - Three-Knot Projection Deforms Shell

Generated by:

```bash
python visualizations/09_generate_outer_shell_deformation.py
```

Outputs:

```text
09_outer_shell_pattern_deforms.gif
09_outer_shell_pattern_deforms_preview.png
```

Meaning:

```text
The stabilized outer shell is no longer perfectly spherical.
Stable 3-knot shapes inside the projection anchor the deformation.
The spectral pattern is preserved while the shell dents around the projected knot shapes.
```

## 10 - Projected Cone Spectrogram

Generated by:

```bash
python visualizations/10_generate_projected_cone_spectrogram.py
```

Outputs:

```text
10_projected_cone_spectrogram.gif
10_projected_cone_spectrogram_preview.png
```

Meaning:

```text
One selected cone is projected into a time-frequency map.
The cone's shell-impact ripple becomes a spectrogram.
Changing core frequency, ripple harmonics, and sidebands are preserved as color intensity.
The spectrogram moves along the time axis in one direction.
```

## 11 - Deformed Shell Shape Over Time

Generated by:

```bash
python visualizations/11_generate_deformed_shell_shape_over_time.py
```

Outputs:

```text
11_deformed_shell_shape_over_time.gif
11_deformed_shell_shape_over_time_preview.png
```

Meaning:

```text
The current deformed shell is shown as a projected boundary.
The shell radius is unrolled as a function of angle.
The unrolled boundary trace moves along the time axis.
This shows the shape evolution of the projected shell directly, separate from the spectrogram.
```

## 12 - 3D Shell Expansion Through Time

Generated by:

```bash
python visualizations/12_generate_shell_expansion_universe_plot.py
```

Outputs:

```text
12_shell_expansion_universe_plot.gif
12_shell_expansion_universe_plot_preview.png
```

Meaning:

```text
The deformed shell is pushed outward and grows.
The shell is shown as repeated cross-sections along a time axis.
The expanding tube preserves the projected three-knot deformation.
This connects shell growth to the standard universe-shape expansion visual.
```

## 13 - Expansion Spectrogram Heatmap

Generated by:

```bash
python visualizations/13_generate_expansion_spectrogram_heatmap.py
```

Outputs:

```text
13_expansion_spectrogram_heatmap.gif
13_expansion_spectrogram_heatmap_preview.png
```

Meaning:

```text
The projected cone spectrogram is overlaid onto the 3D shell expansion history.
Each shell cross-section is colored by cone frequency/ripple intensity at that time.
The expanding universe-shape tube becomes a heatmap of preserved projection history.
```

## 14 - 3D Cubic Model

Generated by:

```bash
python visualizations/14_generate_3d_cubic_model.py
```

Outputs:

```text
14_3d_cubic_model.gif
14_3d_cubic_model_preview.png
```

Meaning:

```text
The cubic model replaces the shell-only view.
A center core connects to six cube faces.
The stable three-knot projection moves inside the cube.
Face panels and connector pulses show how the cubic geometry carries activation.
```

## 15 - 3x3x3 Cubic Grid With Spacetime Scaffold

Generated by:

```bash
python visualizations/15_generate_3x3x3_cubic_spacetime_scaffold.py
```

Outputs:

```text
15_3x3x3_cubic_grid_spacetime_scaffold.png
15_3x3x3_cubic_grid_spacetime_scaffold.gif
```

Meaning:

```text
A static 3x3x3 cubic lattice shows 27 cell centers.
The lattice is overlaid by a spacetime scaffold.
The scaffold includes a time axis, light-cone rings, and body-diagonal causal rays.
This establishes the cubic cell geometry before adding cell-resolved mechanics.
```

## 16 - Spacetime Fabric Twists Through Cubes

Generated by:

```bash
python visualizations/16_generate_spacetime_fabric_twist.py
```

Outputs:

```text
16_spacetime_fabric_twist.gif
16_spacetime_fabric_twist_preview.png
```

Meaning:

```text
The spacetime scaffold is shown as a visible woven fabric.
The 3x3x3 cubic grid remains underneath the fabric.
Cube-to-cube connections tug on the fabric.
The fabric twists and changes local curvature around those connections.
```
