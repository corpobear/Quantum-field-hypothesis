# Addendum v0.29: Collapse-Containment and Coherence Failure

**Status:** speculative paper-style addendum; toy/scaffold result, not established physics.  
**Scope:** documents the v0.29 MCIFT retest, which adds a collapsed-knot containment rule to the v0.28 thermodynamic spin-growth scaffold.

---

## 1. Wording correction

Earlier documentation used the phrase **"no-fit"** too strongly.

The corrected wording is:

```text
no parameter sweep / internally constrained heuristic closure
```

This means the run did not scan parameters to match the target result, but the closure choices are still model assumptions. They are not observationally fitted constants and not first-principle proof.

---

## 2. Motivation

v0.28 improved native shape scoring and preserved the BAO-window scale, but it still left a global long-mode failure:

```text
v0.28 global peak = 617.87 Mpc
```

The hypothesis for v0.29 is that MCIFT was missing a collapse/containment rule: when coherence is no longer sufficient to contain the complexity of a mode, that mode should no longer remain in the free growth spectrum.

---

## 3. Containment rule

Existing MCIFT stability mechanics define complexity and coherence capacity:

```text
C_n = 2^n
S_n = 3.5 n X_n - C_n
```

The physical idea is:

```text
stable structure: coherence capacity >= contained complexity
collapse / containment failure: contained complexity > coherence capacity
```

v0.29 applies this to the thermodynamic cosmology scaffold:

```text
coherence_capacity = 3.5 n X_containment A_lock W_capture
contained_complexity_load = C_n (1 + theta_G + c_s^2)
collapse_excess = max(0, contained_complexity_load - coherence_capacity)
collapse_pressure = max(0, contained_complexity_load / coherence_capacity - 1)
```

For the first retest:

```text
n = 4
C_n = 16
X_containment = 1 + beta_spin
collapse activates only for modes with wavelength lambda > R_A
collapse_factor(lambda) = exp[-collapse_pressure * lambda / R_A]
```

---

## 4. Final containment values

At 5B:

```text
R_A(5B) = 505.829530 Mpc
coherence_capacity = 23.171783
contained_complexity_load = 33.741724
collapse_excess = 10.569941
collapse_pressure = 0.456156
uncontained modes with lambda > R_A = 1
collapse factor at 617.87 Mpc = 0.572816
```

The only uncontained mode in the sampled spectrum is the 617.87 Mpc mode, whose wavelength is larger than the anchor/coherence radius.

---

## 5. Result

```text
v0.28 RMS before collapse = 0.302859
v0.29 RMS after collapse = 0.302859
shape verdict = PASS-LIKE
v0.28 global peak before collapse = 617.87 Mpc
v0.29 global peak after collapse = 152.29 Mpc
v0.29 BAO-window peak = 152.29 Mpc
```

The scored RMS is unchanged because the 617.87 Mpc mode lies outside the v0.28 scoring window. The meaningful change is that the global peak no longer sits at the uncontained super-anchor mode.

---

## 6. Interpretation

The v0.29 result supports the idea that the 617.87 Mpc mode is an uncontained coherence mode rather than a valid final dominant scale.

In MCIFT language:

```text
A collapsed-knot sink forms when coherence capacity is no longer sufficient to contain internal complexity.
```

This does not yet prove black-hole physics. A safer term is:

```text
collapsed-knot reservoir
```

---

## 7. Remaining limitation

v0.29 is still an overlay. It drains the uncontained mode after the v0.28 thermodynamic growth run.

The next required step is dynamic:

```text
add B = collapsed-knot reservoir
rerun conserved A/V/D/R/B background evolution
then rerun thermodynamic perturbation growth
```

Required background form:

```text
d rho_A / d ln a = -Q_A_to_V - Q_A_to_D
d rho_V / d ln a =  Q_A_to_V - Q_V_to_D - Q_V_to_R - Q_V_to_B
d rho_D / d ln a =  Q_A_to_D + Q_V_to_D - Q_D_to_B
d rho_R / d ln a =  Q_V_to_R + collapse thermal feedback
d rho_B / d ln a =  Q_V_to_B + Q_D_to_B
```

---

## 8. Next target

```text
v0.30 = dynamic collapsed-knot reservoir B
        + conserved A/V/D/R/B backgrounds
        + thermodynamic perturbation solver
```

If the dynamic version also suppresses the 617.87 Mpc mode while preserving the BAO-window peak, then the collapse-containment idea becomes a stronger MCIFT-native explanation of the previous long-mode failure.
