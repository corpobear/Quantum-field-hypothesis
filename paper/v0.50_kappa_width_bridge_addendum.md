# v0.50 Kappa / Width Bridge Addendum

**Status:** calibrated bridge layer; speculative MCIFT scaffold, not established physics.

## Purpose

v0.50 replaces rough visible-channel comparison with a standard Higgs kappa-framework bridge.

```text
Gamma_i = kappa_i^2 Gamma_i^SM
BR_i = Gamma_i / Gamma_total
mu(prod,decay) = kappa_prod^2 kappa_decay^2 / kappa_H^2
kappa_H^2 = Gamma_total / Gamma_total^SM
```

## Calibrated benchmark

```text
Gamma_H^SM = 4.070000 MeV
kappa_b = kappa_W = kappa_Z = kappa_g = kappa_tau = kappa_c = kappa_gamma = kappa_mu = 1
BR_BSM = 0
```

This gives:

```text
verdict = PASS_KAPPA_BRIDGE_CALIBRATED
Gamma_total_fit = 4.070000 MeV
kappa_H^2 = 1.000000
max_abs_BR_delta_pct = 0.000000
max_abs_signal_strength_delta_pct = 0.000000
```

## Important boundary

The calibrated kappa bridge matches the SM-like reference point by construction. The raw v0.49 geometry priors do not yet give valid collider kappas in all channels:

```text
kappa_g_prior ≈ 1.132196
kappa_gamma_prior ≈ 0.737311
kappa_mu_prior ≈ 2.974587
```

So v0.50 is a bridge/checkpoint. It is not yet a first-principle MCIFT derivation of LHC Higgs couplings.

## Next target

v0.51 should derive kappa values from the full 3D vector phase field and hidden/core-load geometry without fitting directly to SM branching ratios.
