# v0.54 Cosmology Coarse-Graining Addendum

**Status:** speculative cosmology-scale scaffold; not established physics.

## Purpose

v0.54 promotes the improved local collision mechanics into a first large-scale mathematical layer. It does not fit cosmological data yet. It defines how local MCIFT branches can be coarse-grained into effective density, lapse, sound, rotation, and growth variables.

## Coarse-grained sectors

```text
rho_v      visible matter density
rho_h      hidden/sink density
rho_rot    rotation/vorticity density
rho_s      acoustic/sound-shell density
rho_r      radiation density
N          lapse / clock field
c_s        effective sound speed
```

## Gravity source

```text
rho_grav = rho_v + C_h rho_h + C_rot rho_rot + C_s rho_s + rho_r
```

## Expansion and lapse

```text
H_core(a)^2 = sum_i Omega_i a^(-n_i)
N(a) = tau_rot / sqrt(1 + chi0 H_core(a)^2)
H_lab(a) = H_core(a) / N(a)
```

## Acoustic horizon

```text
r_s(a) = integral_0^a c_s(a') / (a'^2 H_lab(a')) da'
```

## Growth kernel

```text
mu_eff(k) = 1 + hidden_gravity_enhancement(k) + rotation_support(k) - sound_spread_suppression(k)
```

## Result

```text
verdict = PASS_COSMOLOGY_COARSE_GRAIN_SCAFFOLD
criteria_pass_count = 12/12
visible_gravity_fraction_normalized = 0.616356
hidden_gravity_fraction_normalized = 0.178592
rotation_gravity_fraction_normalized = 0.095371
radiation_fraction_normalized = 0.051573
acoustic_shell_fraction_normalized = 0.058107
lapse_N_at_a1 = 0.869985
H_lab_over_H_core_at_a1 = 1.149445
sound_horizon_proxy_at_a1 = 0.394142
```

## Interpretation

The collision mechanics now have a cosmology-scale language:

```text
hidden branch -> gravitational density
sound spread -> pressure/acoustic horizon
time dilation -> lapse field
rotation -> vorticity/support term
channel exchange -> future Q_i transfer terms
```

## Limitation

This is a coarse-graining scaffold, not an observational cosmology fit and not a replacement for standard cosmology.
