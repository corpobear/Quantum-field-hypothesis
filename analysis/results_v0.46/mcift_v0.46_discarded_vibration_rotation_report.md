# MCIFT v0.46 Discarded-Vibration Rotation Strict Test Report

**Status:** dense entangled merge correction after v0.45; speculative scaffold, not established physics.

## Purpose

v0.46 tests the missing term identified after v0.45: the discarded rhythm mismatch should not simply leave the merged object as outward leakage. The discarded vibration carries angular mismatch and should influence the final rotation of the merged dense sphere.

## New rule

```text
E_discarded = E_beat_seed
I_dense = (2/5) M_dense R_dense^2
L_discarded = sqrt(2 I_dense E_rotation)
omega_final = L_discarded / I_dense
```

The rotation generated by the discarded vibration then redistributes part of the over-locked core:

```text
E_core_after_rotation = E_core_seed (1 - rotational_bleed_fraction)
density_pressure_after_rotation < density_pressure_before_rotation
core_pressure = (E_core_after_rotation + density_pressure_after_rotation) / coherence_capacity
```

## Strict result

```text
verdict = PARTIAL_ROTATION_RELIEF
criteria_pass_count = 16/17
R_dense = 6.198812
rho_ratio = 1.813671
M_dense = 3.093656
coherence_capacity = 0.770210
E_core_seed_before_rotation = 1.496605
E_beat_seed_discarded_vibration = 0.188784
torque_fraction_from_discarded_vibration = 0.698456
L_discarded_vibration = 3.423467
E_rotation_from_discarded_vibration = 0.131844
omega_final = 0.072027
rotational_bleed_fraction = 0.572734
E_core_after_rotation = 0.639429
density_pressure_after_rotation = 0.183042
core_pressure_after_rotation = 1.066881
final_energy_over_peak = 0.318000
```

## Shape result

```text
weighted_shell_radius = 9.386959
weighted_shell_width = 2.180000
weighted_sphericity = 0.792000
weighted_anisotropy = 0.208000
core_fraction = 0.512000
inner_fraction = 0.312000
outer_fraction = 0.176000
```

## Channel fractions

```text
bb_like     = 0.564218  target ~ 0.582000
WZ_like     = 0.244806  target ~ 0.240000
gg_like     = 0.126940  target ~ 0.086000
tau_like    = 0.061724  target ~ 0.063000
gamma_like  = 0.001107  target ~ 0.002300
mumu_like   = 0.001205  target ~ 0.000220
```

## Criteria

```text
dense_radius_smaller_than_volume_radius = True
density_ratio_above_one = True
discarded_vibration_nonzero = True
discarded_vibration_converted_to_rotation = True
final_rotation_nonzero = True
angular_momentum_from_discarded_vibration_positive = True
core_pressure_below_capacity_after_rotation = False
density_pressure_reduced_by_rotation = True
shell_detaches_outside_dense_radius = True
final_energy_not_runaway = True
sphericity_remains_reasonable = True
anisotropy_present_not_dominant = True
bb_like_largest = True
WZ_like_visible_not_dominant = True
gg_like_visible_not_dominant = True
gamma_mumu_suppressed = True
channel_l1_below_0p20 = True
```

## Interpretation

```text
The discarded vibration term is real and important, but it does not fully stabilize the dense merge under the current strict rule. Compared with v0.45, the discarded beat creates angular impulse, producing final rotation of the merged dense sphere. That rotation lowers the overload strongly and restores a near target channel hierarchy. However, core pressure remains slightly above capacity, so this is not a strict pass. The missing piece is now narrower: rotation must either increase coherence capacity or bleed a little more core pressure through a first-principle angular shell rule.
```

## Limitation

```text
This is still a toy field calculation, not empirical collider simulation. PARTIAL_ROTATION_RELIEF means the discarded-vibration-to-rotation rule helps inside this scaffold but does not yet pass the hard containment criterion.
```

## Next target

```text
v0.47 target:
derive the discarded-vibration angular impulse directly from the 3D vector phase field and let rotation feed back into coherence capacity, without tuning decay channels.
```
